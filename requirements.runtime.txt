# ─────────── NPGPT inference (CPU-only) ───────────
# Minimal wheels required by test_ligand_generation.py
# and Smiles-GPT checkpoint loading.  No CUDA libs.

torch==2.4.0+cpu            --index-url https://download.pytorch.org/whl/cpu
lightning==2.4.0
pydantic==2.9.2
transformers==4.45.2
scikit-learn==1.5.2
torchmetrics==1.4.2         # tiny, required by lightning
rdkit==2024.9.6             # SMILES validation
gdown==5.2.0                # Google-Drive downloader

# optional telemetry (uncomment if you use it)
# wandb==0.18.3
